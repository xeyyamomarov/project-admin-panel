export const setItemLocalStorage = (key, item) => window.localStorage.setItem(key, item);
export const getItemLocalStorage = (key) => window.localStorage.getItem(key);
export const removeItemLocalStorage = (key) => window.localStorage.removeItem(key);